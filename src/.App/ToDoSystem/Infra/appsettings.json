{
  "ConnectionStrings": {
    // Application database
    "DefaultConnectionSqlite": "Data Source=${DATABASES_PATH}\\todo.db",
    "DefaultConnectionPostgres": "Host=localhost;Port=5432;Database=todo;Username=postgres;Password=admin",

    // Serilog - PostgreSQL sink
    "LogConnection": "${ConnectionStrings:DefaultConnectionPostgres}",

    // Hangfire
    "HangfireSqlite": "${DATABASES_PATH}\\hangfire.db",
    "HangfirePostgres": "Host=localhost;Port=5432;Database=hangfire;Username=postgres;Password=admin"
  },
  "OpenApi": {
    "Title": "ToDo System API",
    "Description": "API para gerenciamento de tarefas (ToDo)."
  }
}